name: Resolve Outdated Comments
on:
  pull_request:
    types: [synchronize]

permissions:
  pull-requests: write

jobs:
  resolve:
    runs-on: ubuntu-latest
    steps:
      - uses: jpoehnelt/resolve-comments@main
